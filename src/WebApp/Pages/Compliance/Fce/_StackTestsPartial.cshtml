@model List<StackTestWork>
@using Domain.Compliance.Models.WorkItems

<h2>Source Tests</h2>

@{
    if (Model is not { Count: > 0 })
    {
        <div class="fce-content">None.</div>
        return;
    }

    var firstItem = Model.FirstOrDefault()!;
}

<table class="">
    <thead>
        <tr>
            <th scope="col" class="no-wrap">@Html.DisplayNameFor(m => firstItem.Id)</th>
            <th scope="col">@Html.DisplayNameFor(m => firstItem.ReferenceNumber)</th>
            <th scope="col">@Html.DisplayNameFor(m => firstItem.ReceivedDate)</th>
            <th scope="col">@Html.DisplayNameFor(m => firstItem.Reviewer)</th>
            <th scope="col">@Html.DisplayNameFor(m => firstItem.ComplianceStatus)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var i in Model)
        {
            <tr>
                <th scope="row" rowspan="2">@i.Id</th>
                <td>@i.ReferenceNumber</td>
                <td>@Html.DisplayFor(m => i.ReceivedDate)</td>
                <td>@i.Reviewer.SortableFullName</td>
                <td>@i.ComplianceStatus</td>
            </tr>
            <tr>
                <td colspan="4">
                    <dl class="dl-inline">
                        <dt>@Html.DisplayNameFor(m => firstItem.SourceTested)</dt>
                        <dd>@i.PollutantMeasured – @i.SourceTested</dd>
                    </dl>
                </td>
            </tr>
        }
    </tbody>
</table>
