@model Address
@using Domain.ValueObjects

<div class="text-nowrap" aria-label="Postal address">
    @Model.Street
    @if (!string.IsNullOrWhiteSpace(Model.Street2))
    {
        <br />
        @Model.Street2
    }
    <br />
    @Model.City, @Model.State @Model.PostalCode
</div>
